# virid

`virid` 是一个用`TypeScript`语言编写的为重型electron应用而生的**确定性消息驱动引擎**。它将业务逻辑从 UI 框架的碎屑中彻底剥离，构建出一个具备**位置透明性**的微型分布式核心。

此仓库为`virid`的`python`移植版（正在工作中），旨在为混乱的python控制流提供强确定性、同步、解耦的状态管理与控制工具。

## 模块组成

| **模块**         | **定位**         | **关键特性**                                   |
| ---------------- | ---------------- | ---------------------------------------------- |
| **`virid.core`** | **核心**         | **确定性 Tick 机制、双缓冲消息池、依赖注入。** |
| **virid.std**    | **核心功能增强** | **提供消息事务、时序控制等等**                 |

## 其他

👉 **[Virid 本体](https://github.com/Ailrid/virid)** – 使用`TypeScript`编写的完整的`virid`框架。

