[project]
name = "virid"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "core",
    "std",
]

[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
core = { workspace = true }
std = { workspace = true }