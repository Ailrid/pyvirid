[project]
name = "std"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "core",
]

[tool.uv.sources]
core = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["src/virid"]
layout = "src"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"